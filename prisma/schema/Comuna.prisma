//REVISADO
model Comuna {
  id         Int     @id @default(autoincrement())
  provinciaId Int
  regionId   Int
  nombre     String
  Old_Id     Int?    // Campo legacy para migración

  region     Region  @relation(fields: [regionId], references: [id])
  plantas    Planta[]

  @@map("Comuna")
}