//REVISADO - Estructura basada en AspNetUserLogins
model UsuarioLogin {
  id                  String  @id @default(cuid())
  loginProvider       String
  providerKey         String
  providerDisplayName String?
  userId              String

  // Relación con Usuario
}
