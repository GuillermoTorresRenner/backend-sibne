//REVISADO
model ArchivoAdjunto {
  id            Int    @id @default(autoincrement())
  nombreArchivo String
  tipo          String
  ext           String
  fileData      Bytes

  @@map("ArchivoAdjunto")
}