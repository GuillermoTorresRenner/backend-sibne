//REVISADO
model Provincia {
  id       Int      @id @default(autoincrement())
  nombre   String
  regionId Int
  region   Region   @relation(fields: [regionId], references: [id])
  comunas  Comuna[]

  @@map("Provincia")
}
